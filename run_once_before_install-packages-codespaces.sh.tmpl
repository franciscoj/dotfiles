{{- $codespaces:= env "CODESPACES" | not | not -}}
{{- if $codespaces -}}
#!/bin/sh

apt-get install -y libssl-dev zsh squashfuse exa jq ripgrep git-extras
sh -c "$(curl -fsSL https://starship.rs/install.sh)"
chsh -s /bin/zsh root
{{end -}}
