# vim: set ft=sh

alias _="sudo"
alias rake="noglob rake"

alias e="${(z)VISUAL:-${(z)EDITOR}}"
alias vi=$EDITOR

alias tmux="tmux -u"

alias ll="exa -lh"
alias la="exa -lha"
alias lg="exa -lha --git"

alias be="bundle exec"
