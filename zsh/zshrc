export ZSH=$HOME/.oh-my-zsh
export ZSH_THEME="sunrise"
plugins=(git github gem rails rbenv heroku)
source $ZSH/oh-my-zsh.sh

export PATH="$HOME/bin:/usr/local/bin:$PATH:/usr/local/sbin"
export PATH="/usr/local/heroku/bin:$PATH"
export HIPCHAT_USER='Fran Casas'
export EDITOR='emacs -nw'
export DISABLE_AUTO_TITLE=true

# Aliases
alias be='bundle exec'
alias rake='noglob bundle exec rake'
alias migrate='rake db:migrate db:test:clone'
alias rspec='bundle exec rspec'
alias rp='rake parallel:spec[3]'
alias r='rspec'
alias nrr='RSPEC_NO_RETRY=true r'
alias p='teamocil --here'
alias e='emacs -nw'

[[ -f $HOME/.gemrc_local ]] && export GEMRC=$HOME/.gemrc_local
[[ -s $HOME/.nvm/nvm.sh ]] && . $HOME/.nvm/nvm.sh
[[ -f $HOME/.zshrc ]] && source $HOME/.zshrc_local
if which rbenv > /dev/null; then eval "$(rbenv init -)"; fi
